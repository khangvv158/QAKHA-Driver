package com.example.qakhadriver.screens.container.adapter

import androidx.fragment.app.FragmentManager
import com.example.qakhadriver.widget.viewpager.PagerAdapter

class ContainerPagerAdapter(fm: FragmentManager) : PagerAdapter(fm)
